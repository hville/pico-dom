Shared Context
	cf({init: function () { this.sharedContext = {} }})
	vs
	cf({context: {}})


Update comparison
	.isDataChange(data) {
		if (this.last === data) return true
		this.last = data
	}
	if (this.isDataChange(v)) this.edit()...
!!! not for lists !!!
!!! a new sort order would have the same data but different index !!!


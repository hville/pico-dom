review merge across CE, and CO
* rename merge2 assignL2

review cloning
- child elements must be passed (eg. list)
- deep clone
- content must be rebuilt
- elements/items must not be taken away from ole elements

matrix li test


TODO
- el, co, li share the same Argument Parser:
SELECTOR
* e => e
* s|0 => o (selector parse)
* f => f
OPTIONS
* o => o'
CONTENT
* e => e
* s => s
* f => f

TODO
- el, co, li share the same nested Functions Creator
(C, d) => (e, o, c) => (i) => C(e, o&d, c)
Element: return setChildren(decorate(e||f(o), o), c)
Co: new C(); return ctx.view


